import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  msg: {
    fontFamily: 'Nunito',
    padding: '80px',
    marginBottom: '400px',
  },
});

export default useStyles;
